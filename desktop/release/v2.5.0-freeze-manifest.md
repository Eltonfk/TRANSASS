# Transass v2.5.0 — manifesto de congelamento

Data do registro: `2026-09-03`

## Identidade do snapshot

| Campo | Valor |
|---|---|
| Tag | `v2.5.0` |
| Commit | `b5166b937d284f38edf1f43a9ec3c9c9261bd2ba` |
| Branch de origem | `candidate/v2.3.8` |
| Mensagem | `feat(web): improve translation workflow UX` |
| Tipo de tag | annotated tag |
| Arquivo lógico | `transass-v2.5.0/` |

O checksum abaixo foi calculado com:

```sh
git archive --format=tar --prefix=transass-v2.5.0/ v2.5.0 | sha256sum
```

| Artefato | SHA-256 |
|---|---|
| arquivo tar lógico do snapshot | `5ed9a890afeade9516e41ab317802cd6e4058490e33d20fda5abd995ac32bdd0` |
| `requirements.lock` | `783ebc5dddc7b7783c489e0314179964a315cdf7b1e33c9136f6f83931c7f985` |
| `.env.example` | `4cb5082ff0a9a50c6722c713fb0667ef404d169dd9949c37362772202cc42ae7` |
| `deploy/Dockerfile` | `08c2cba3a1e060ce2d6e1ea39aa6d88eb7b0ce91484963b46a183062a1f81f4a` |

## Gate de testes

Status: **PASS no gate padrão do produto; perfis históricos/stress separados**.

- Gate padrão (`pytest tests/offline`): `903 passed`, `38 deselected`,
  `198 subtests passed`, sem warnings.
- Testes Desktop de paths: `2 passed` (incluídos no gate padrão).
- O gate padrão não inclui os quatro módulos históricos E07 nem o stress de
  233 chamadas.
- O perfil histórico foi executado separadamente e retornou `4 failed`, `18
  errors`, `15 passed`, confirmando a ausência intencional das evidências.
- O perfil stress permanece separado por custo de I/O; seus `fsync` não foram
  desativados nem relaxados.

### Erros conhecidos reproduzidos

Os planners offline B5, B6, B7 e o planner de batches dependem de evidências
runtime protegidas em `anime-subtitle-translator-review`. Os arquivos-fonte
esperados (`e07.ass`) não estão presentes nesta worktree/ambiente. Por isso,
os testes de prontidão terminam em `B5_SOURCE_NOT_FOUND`,
`B6_SOURCE_NOT_FOUND`, `B7_SOURCE_NOT_FOUND` ou `BATCH_SOURCE_NOT_FOUND`.
Isso é uma ausência de fixture/evidência externa, não uma autorização para
relaxar o fail-closed ou fabricar uma fonte substituta.

O teste de 233 chamadas não apresentou deadlock: o stack trace capturado mostra
tempo concentrado em `fsync` de arquivos e diretórios durante a persistência
durável. A execução excede o limite interativo deste ambiente antes de chegar
ao resumo.

Evidência histórica anterior do projeto não substitui uma execução atual e é
mantida apenas como histórico.

## Divergências conhecidas

- `_version.py` declara `2.5.0`, mas o `pyproject.toml` do snapshot declara
  `2.4.9`.
- O snapshot congelado não contém as alterações não commitadas da linha
  Desktop presentes na worktree atual.
- A tag `v2.5.0` não foi reescrita nem movida.

## Escopo e segurança

- Nenhuma chamada real de modelo foi feita.
- Nenhuma Library, state real ou produção foi alterada.
- Nenhuma promoção para `main`, deploy ou push remoto foi feita.
- O manifesto é aditivo e não altera evidências históricas.

## Decisão de continuidade

A linha Desktop pode continuar a partir do commit indicado. O gate do produto
está liberado; os perfis `historical` e `stress` continuam visíveis e devem ser
executados separadamente quando suas condições estiverem disponíveis.
