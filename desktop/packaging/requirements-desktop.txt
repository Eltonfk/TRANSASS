# Runtime GUI, embedded QtWebEngine view and optional OS credential vault.
PySide6>=6.7,<7
keyring>=25,<26

# Build-only tools (not installed in the final bundle at runtime).
pyinstaller>=6.10,<7
Pillow>=10,<12
