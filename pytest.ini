[pytest]
testpaths = tests/offline
python_files = test_*.py
addopts = -ra -m "not historical and not stress"
markers =
    historical: depends on intentionally archived external runtime evidence
    stress: intentionally expensive durability/stress coverage
